export type prodArr = { name: string; amount: number; level: number }[];

export type Area = {
	id: number;
	prodArr: prodArr;
};

export type ProdArr = {
	name: string;
	amount: number;
	level: number;
}[];
